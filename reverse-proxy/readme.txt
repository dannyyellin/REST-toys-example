Do NOT use this directory.  Use load-balance instead.
Hence:
app directory is simple toy service without persisatent
webServer introduces docker compose, mongo, volumes
load-balance introduces nginx, restart, loadbalancing,...